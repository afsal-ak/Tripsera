export enum EnumMessageType {
  TEXT = "text",
  IMAGE = "image",
  FILE = "file",
  BLOG = "blog",
  PACKAGE = "package",
  AUDIO = "audio",
  CALL = "call",
    VIDEO = "video",

}

export enum EnumCallType {
  AUDIO = "audio",
  VIDEO = "video",
  CALL='call'
}

export enum EnumCallStatus {
  INITIATED = "initiated",
  ANSWERED = "answered",
  MISSED = "missed",
  ENDED = "ended",
  REJECTED = "rejected",
  CANCELLED = "cancelled",
}
