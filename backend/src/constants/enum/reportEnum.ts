
export enum EnumReportedType {
  BLOG = "blog",
  REVIEW = "review",
  USER = "user",
}

export enum EnumReportStatus {
  PENDING = "pending",
  RESOLVED = "resolved",
  DISMISSED = "dismissed",
}

export enum EnumAdminAction {
  WARN = "warn",
  BLOCK = "block",
  DELETE = "delete",
  NONE = "none",
}

export enum EnumReportBlockStatus{
    BLOCK='block',
    ACTIVE='active'
}